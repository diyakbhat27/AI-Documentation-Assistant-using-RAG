version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./chroma_db:/app/chroma_db   # vector store persists
      - ./data:/app/data             # SQLite persists
      - ./cache:/app/cache           # crawl cache persists
